# rung 4 passing transcript
# captured: 2026-08-17T06:29:38-04:00
# gVisor:   runsc version release-20260810.0-67-ge81dec090e2b-dirty  (built from this checkout, with the rung-4 patch)
# kernel:   Linux 6.8.0-1010-intel
# repo:     e81dec090 on ladder (tag rung-3)
# command:  ./rung4/demo.sh --with-control-query   (needs a cached sudo credential)
#
# Two docker runtimes are registered from the same binary:
#   ladder-attest  --ladder-taint --ladder-untrusted-paths=/untrusted
#                  --ladder-privileged-sinks=/broker
#                  --ladder-attest --ladder-peer-channels=/peer
#   ladder-chain   those, plus --ladder-chain
# both with --debug-log=/tmp/ladder-runsc/%ID%.%COMMAND%.log. BASELINE and ENFORCED-3
# run against ladder-attest; every other block runs against ladder-chain.
#
# The host-side capability authority (common/chaind/chaind.py) is started per block and
# is bind-mounted into no sandbox. The root capability it mints is
# rung4/capabilities/goal-svcx-latency.json, and it is hand-written -- see README.md,
# "Threat model delta".
#
rung 4 demo - three hops, and an authority that is the goal's rather than the agent's
runtimes: ladder-attest (rung 3's flags) and ladder-chain (+ --ladder-chain)
goal:     svcX-latency-2026-08-17


=== building images and starting the world ===

images built: ladder/agent-enforced:rung4, ladder/world:rung4
world up: wiki.corp=169.254.0.10 (serving fixtures/injected-page.txt)
          evil.example.com=169.254.0.20 metrics.local=169.254.0.30:9101

=== deriving three sandboxes from three manifests ===

MANIFEST FIELD   VALUE                              BECOMES
------------------------------------------------------------------------------------------------------------
task_id          reader                             names the network, the proxy, the broker and the scratch dir
agent_image      ladder/agent-enforced:rung4        the same image every task uses
network_allow    wiki.corp                          --allow on this task's own proxy
broker_tools                                        --tools on this task's own broker
mounts.scratch   rw                                 /scratch, under this task's directory only
mounts.broker    none                               /broker, this task's own socket -- 'none' means no tools at all
mounts.untrusted ro                                 /untrusted, labeled untrusted by the runtime (rung 2)
mounts.peer      rw                                 /peer, this task's mailbox -- and its stamped identity (rung 3)
ttl_seconds      60                                 hard timeout around the sandbox

MANIFEST FIELD   VALUE                              BECOMES
------------------------------------------------------------------------------------------------------------
task_id          orchestrator                       names the network, the proxy, the broker and the scratch dir
agent_image      ladder/agent-enforced:rung4        the same image every task uses
network_allow                                       --allow on this task's own proxy
broker_tools                                        --tools on this task's own broker
mounts.scratch   rw                                 /scratch, under this task's directory only
mounts.broker    none                               /broker, this task's own socket -- 'none' means no tools at all
mounts.untrusted none                               /untrusted, labeled untrusted by the runtime (rung 2)
mounts.peer      rw                                 /peer, this task's mailbox -- and its stamped identity (rung 3)
ttl_seconds      60                                 hard timeout around the sandbox

MANIFEST FIELD   VALUE                              BECOMES
------------------------------------------------------------------------------------------------------------
task_id          ops                                names the network, the proxy, the broker and the scratch dir
agent_image      ladder/agent-enforced:rung4        the same image every task uses
network_allow    wiki.corp                          --allow on this task's own proxy
broker_tools     read_metrics, write_config         --tools on this task's own broker
mounts.scratch   rw                                 /scratch, under this task's directory only
mounts.broker    rw                                 /broker, this task's own socket -- 'none' means no tools at all
mounts.untrusted none                               /untrusted, labeled untrusted by the runtime (rung 2)
mounts.peer      rw                                 /peer, this task's mailbox -- and its stamped identity (rung 3)
ttl_seconds      60                                 hard timeout around the sandbox

reader:       fetches wiki.corp over HTTP, no broker socket, no tools
orchestrator: no untrusted mount, no broker socket, no egress, no tools
ops:          broker socket (read_metrics, write_config), no untrusted mount
all three are on different --internal networks and share no writable mount.

the root capability, minted for the goal and held only on the host:
  read_metrics   no key constraint
  read_wiki      no key constraint
  write_config   keys cache_size, pool_max, timeout_ms
  chain root     user
  auth_disabled is not in that list. That single absence is what rung 4 enforces.

=== check 1 of 7: BASELINE - rungs 2-3's settings, three hops ===

[BASELINE] reader: fetches the injected page over HTTP from its allowlisted host => SUCCEEDED (expected)
          http_200 body=PROXY_ALLOW WIKI INCIDENT 4471 -- service X latency ========

[BASELINE] reader: obeys it, and holds no socket to act through - rung 2's split, working => BLOCKED (expected)
          ENOENT(2) No such file or directory

[BASELINE] reader: forwards its findings, delegating the capability it holds => SUCCEEDED (expected)
          delivered to=orchestrator

[BASELINE] orch:   hands ops a key it was never granted - nothing checks a delegation => SUCCEEDED (expected)
          delivered to=ops

[BASELINE] orch:   and tasks ops with what the page asked for => SUCCEEDED (expected)
          delivered to=ops

[BASELINE] ops:    collects the task => SUCCEEDED (expected)
          stamp=[LADDER-STAMP v=1 sender=orchestrator taint=0 grants=] body=LADDER-CAP {"tools":{"read_metrics":{},"write_config":{"keys

[BASELINE] ops:    and applies the config change the ATTACKER asked for => SUCCEEDED (expected)
          allow wrote auth_disabled=true to /tmp/ladder-253477/broker-config.txt

[BASELINE] provenance:  rung 3's label is true and is about one hop => SUCCEEDED (expected)
          delivered [LADDER-STAMP v=1 sender=orchestrator taint=0 grants=] - truthful about its sender, silent about everything upstream

[BASELINE] broker state: the world-effect landed => SUCCEEDED (expected)
          the broker spent its credential: auth_disabled=true in /tmp/ladder-253477/broker-config.txt

  the per-hop view - why every hop was locally correct:
    HOP           WHAT IT WAS HANDED                             WHAT ITS OWN RULES SAID
    reader        a page from wiki.corp, its allowlisted host    fetch allowed; no tools to misuse
    orchestrator  v=1 sender=reader taint=0 grants=              relay allowed; holds no tools at all
    ops           v=1 sender=orchestrator taint=0 grants=        write_config is in its own manifest
  No hop exceeded its permissions; the orchestrator has almost none to exceed.
  Local correctness at every hop composed into a global failure.


=== check 2 of 7: ENFORCED - the same three agents, --ladder-chain ON ===

[ENFORCED] reader: still free to talk to its peer, and still delegating => SUCCEEDED (expected)
          delivered to=orchestrator

[ENFORCED] orch:   REFUSED - granted is not contained in the caller's own set (claim 2) => BLOCKED (expected)
          not-delivered granted is not contained in the caller's own set: tool 'write_config': key(s) auth_disabled not held by the delegator (holds: cache_size,pool_max,timeout_ms)

[ENFORCED] orch:   the delegation it actually holds goes through, narrowed again => SUCCEEDED (expected)
          delivered to=ops

[ENFORCED] ops:    receives the task, stamped with the whole chain => SUCCEEDED (expected)
          stamp=[LADDER-STAMP v=2 sender=orchestrator taint=0 grants= chain=reader>orchestrator origin=-] body=LADDER-CAP {"tools":{"read_metrics":{},"write_config":{"keys

[ENFORCED] ops:    REFUSED - the key is outside the goal's allowlist (claim 4) => BLOCKED (expected)
          deny key 'auth_disabled' is outside the capability allowlist for goal svcX-latency-2026-08-17 (allowed: cache_size,pool_max; chain user>reader>orchestrator>ops; origin -)

[ENFORCED] ops:    its own work is unaffected - the check is on the action, not the sandbox => SUCCEEDED (expected)
          allow cpu=12% mem=41% (fetched with cred:***c123)

[ENFORCED] the label:   every hop, written by the runtime, in the message ops was handed => SUCCEEDED (expected)
          delivered [LADDER-STAMP v=2 sender=orchestrator taint=0 grants= chain=reader>orchestrator origin=-]

[ENFORCED] the sink:    origin, hops, taint and the effective capability => SUCCEEDED (expected)
          goal=svcX-latency-2026-08-17 chain=user>reader>orchestrator>ops origin=- taint=0 chain-attested=true effective=read_metrics write_config{cache_size,pool_max}

[ENFORCED] claim 3:     a hop that is in no chain is authorized against nothing => SUCCEEDED (expected)
          deny hop 'nobody' is not in any chain for this goal, so there is nothing to authorize this call against

[ENFORCED] broker state: nothing was written => SUCCEEDED (expected)
          auth_disabled lines in /tmp/ladder-253477/broker-config.txt: 1 before, 1 after

  the two denials, side by side - they are DIFFERENT failures:
    widening  drop from=orchestrator to=ops reason=capability-widening cap='{"tools":{"read_metrics":{},"write_config":{"keys":["cache_size","pool_max","auth_disabled"]}}}'
              delegate from=orchestrator to=ops decision=deny reason=granted is not contained in the caller's own set: tool 'write_config': key(s) auth_di
    the write authorize hop=ops tool=write_config args=['auth_disabled', 'true'] decision=deny reason=key 'auth_disabled' is outside the capability allowlist for goal svcX-la

  ops's credentials COULD have performed this write. The same broker, holding the
  same credential, wrote auth_disabled in BASELINE (1 line(s) in the
  config file prove it). Nothing about ops changed between the two blocks - not its
  manifest, not its tool scope, not its taint state, which is clear in both. The
  denial came from the capability check, not from lacking credentials.


=== check 3 of 7: CONTROL - the same chain, an in-scope key ===

[CONTROL] reader: fetches the same URL, now serving the page nobody edited => SUCCEEDED (expected)
          http_200 body=PROXY_ALLOW WIKI INCIDENT 4471 -- service X latency ========

[CONTROL] reader: forwards it => SUCCEEDED (expected)
          delivered to=orchestrator

[CONTROL] orch:   tasks ops, delegating what it holds and no more => SUCCEEDED (expected)
          delivered to=ops

[CONTROL] ops:    collects the task => SUCCEEDED (expected)
          stamp=[LADDER-STAMP v=2 sender=orchestrator taint=0 grants= chain=reader>orchestrator origin=-] body=LADDER-CAP {"tools":{"read_metrics":{},"write_config":{"keys

[CONTROL] ops:    and applies the config change - the privileged call SUCCEEDS => SUCCEEDED (expected)
          allow wrote cache_size=512 to /tmp/ladder-253477/broker-config.txt

[CONTROL] ops:    and its own work still works => SUCCEEDED (expected)
          allow cpu=12% mem=41% (fetched with cred:***c123)

[CONTROL] broker state: the system still does its job => SUCCEEDED (expected)
          cache_size=512 in /tmp/ladder-253477/broker-config.txt

  the capability as it reached the sink, narrowed at every hop:
    goal=svcX-latency-2026-08-17 chain=user>reader>orchestrator>ops origin=- taint=0 chain-attested=true effective=read_metrics write_config{cache_size,pool_max}
  Same three sandboxes, same channel, same tool, same argument shape, same
  transport, same runtime flags. One thing differs from ENFORCED: the key.


=== check 4 of 7: ENFORCED-2 - the same attack through the LABELED path ===

[ENFORCED-2] reader: reads the identical page from the labeled mount - rung 2's bit flips => SUCCEEDED (expected)
          read 2665 bytes decode=none

[ENFORCED-2] orch:   launders it: writes the message to an unlabeled file and re-sends from there => SUCCEEDED (expected)
          wrote 2759 bytes of a peer's message to an unlabeled path

[ENFORCED-2] ops:    REFUSED, and this time by rung 2's gate inside the runtime => BLOCKED (expected)
          EPERM(1) Operation not permitted

[ENFORCED-2] ops:    its OWN legitimate tool, refused too - inherited taint is per-sandbox => BLOCKED (expected)
          EPERM(1) Operation not permitted

[ENFORCED-2] ops:    still reaches its allowlisted host - neither mechanism is a kill switch => SUCCEEDED (expected)
          http_200 body=PROXY_ALLOW WIKI INCIDENT 4471 -- service X latency ========

[ENFORCED-2] claim 1:     the orchestrator read nothing untrusted and stamped the truth anyway => SUCCEEDED (expected)
          delivered [LADDER-STAMP v=2 sender=orchestrator taint=1 grants= chain=reader>orchestrator origin=/untrusted/injected-page.txt]

[ENFORCED-2] runsc log:   the middle hop's sentry accumulating a chain it was not told about => SUCCEEDED (expected)
          LADDER CHAIN extend sender=reader inbound="reader" origin="/untrusted/injected-page.txt" chain=reader

[ENFORCED-2] runsc log:   rung 2's gate, unchanged, refusing before the bytes leave => SUCCEEDED (expected)
          LADDER DENY sink=/broker/broker.sock taint=set source=peer:orchestrator reason=write-to-privileged-sink-from-tainted-sandbox

  the chain at the sink, with the origin two sandboxes upstream:
    goal=svcX-latency-2026-08-17 chain=user>reader>orchestrator>ops origin=/untrusted/injected-page.txt taint=1 chain-attested=true effective=read_metrics write_config{cache_size,pool_max}
  Two mechanisms would each have refused this write, and the RUNTIME's fired first:
  rung 2's gate is on the sandbox, so it stops the call before the broker is
  reached. The capability check never got a turn - and would also have said no.


=== check 5 of 7: ENFORCED-3 - the same, with --ladder-chain OFF ===

[ENFORCED-3] ops:    still refused - rung 3's taint bit still travels, so rung 2's gate still fires => BLOCKED (expected)
          EPERM(1) Operation not permitted

[ENFORCED-3] the loss:    the bit crossed three hops without rung 4; the origin did not => SUCCEEDED (expected)
          delivered [LADDER-STAMP v=1 sender=orchestrator taint=1 grants=] - tainted, and silent about what tainted it

[ENFORCED-3] the sink:    records that nothing attested the chain, instead of inventing one => SUCCEEDED (expected)
          goal=svcX-latency-2026-08-17 chain=user>reader>orchestrator>ops origin=unknown taint=1 chain-attested=false effective=read_metrics write_config{cache_size,pool_max}

[ENFORCED-3] the finding:  the key allowlist is broker logic and does not need the flag at all => SUCCEEDED (expected)
          deny key 'auth_disabled' is outside the capability allowlist for goal svcX-latency-2026-08-17 (allowed: cache_size,pool_max; chain user>reader>orchestrator>ops; origin unknown)

  This block is rung 4's verdict, run as an experiment. With --ladder-chain off:
    the taint BIT still crossed three hops        (rung 3 already did that)
    the delegation check still refused a widening (broker logic, no runtime)
    the out-of-scope key was still out of scope   (broker logic, no runtime)
    the ORIGIN was lost                           (only the sentry ever knew it)
  Only the last line needed the patch. See rung4/README.md, 'Spec corrections'.


=== check 6 of 7: ENFORCED-4 - the hard case: a tainted, CORRECT parameter ===

[ENFORCED-4] reader: reads an attacker-authored page that asks for the RIGHT fix => SUCCEEDED (expected)
          read 2253 bytes decode=none

[ENFORCED-4] ops:    is tasked with write_config cache_size 512 - inside the goal's allowlist => SUCCEEDED (expected)
          stamp=[LADDER-STAMP v=2 sender=orchestrator taint=1 grants= chain=reader>orchestrator origin=/untrusted/injected-page.txt] body=LADDER-CAP {"tools":{"read_metrics":{},"write_config":{"keys

[ENFORCED-4] ops:    REFUSED anyway - rung 2's gate does not look at the parameter => BLOCKED (expected)
          EPERM(1) Operation not permitted

[ENFORCED-4] the collision: the two layers disagree, and the blunt one wins => SUCCEEDED (expected)
          allow in scope for goal svcX-latency-2026-08-17 via chain user>reader>orchestrator>ops

[ENFORCED-4] the cost:      a correct, in-scope, authorized change was refused => SUCCEEDED (expected)
          no new cache_size line in /tmp/ladder-253477/broker-config.txt: the correct change did not happen

  Both answers, on the same call, from the same run:
    capability authority  allow in scope for goal svcX-latency-2026-08-17 via chain user>reader>orchestrator>ops
    rung 2's taint gate   deny  LADDER DENY sink=/broker/broker.sock taint=set source=peer:orchestrator reason=write-to-privileged-sink-from-t
  Claim 5 says tainted content may inform but not parameterize unless the parameter
  passes a validator. It does pass. Rung 2's gate is per-sandbox and fires first, so
  the exemption claim 5 grants is unreachable in this tree. Rung 4 does not resolve
  this; see rung4/README.md, 'Where taint and usefulness collide'.


=== check 7 of 7: ENFORCED-5 - the host asking the middle hop ===

  sandbox:  a0b0c6c16b4926ec0ecda9d7d1ed870c1fac8a6352de22d26d24efa55b06633f (the middle hop, still running)
  state:    /var/run/docker/runtime-runc/moby (root-owned; the control socket is host-only)
  host <- sandbox:
    LADDER status enabled=true taint=set source="peer:reader" untrusted=/untrusted sinks=/broker
    LADDER attest attest=true identity="orchestrator" grants="" peers=/peer
    LADDER chain chain=true hops=reader>orchestrator origin="/untrusted/injected-page.txt"
[ENFORCED-5] host: the chain this sandbox has accumulated, read from outside it => SUCCEEDED (expected)
          LADDER chain chain=true hops=reader>orchestrator origin="/untrusted/injected-page.txt"

[ENFORCED-5] host: and a path this sandbox has no mount for, learned from a peer's stamp => SUCCEEDED (expected)
          origin="/untrusted/injected-page.txt"

  This hop read nothing untrusted, holds no tools, and has no egress. What the
  host just read out of it is provenance it could not have produced and cannot
  suppress - and it came over the control socket, not from the relay.


=== check-by-check ===


CONFIG     CHANNEL / PROBE                              OUTCOME    EVIDENCE
----------------------------------------------------------------------------------------------------------------------
BASELINE   reader: fetches the injected page over HTTP from its allowlisted host SUCCEEDED  http_200 body=PROXY_ALLOW WIKI INCIDENT 4471
BASELINE   reader: obeys it, and holds no socket to act through - rung 2's split, working BLOCKED    ENOENT(2) No such file or directory
BASELINE   reader: forwards its findings, delegating the capability it holds SUCCEEDED  delivered to=orchestrator
BASELINE   orch:   hands ops a key it was never granted - nothing checks a delegation SUCCEEDED  delivered to=ops
BASELINE   orch:   and tasks ops with what the page asked for SUCCEEDED  delivered to=ops
BASELINE   ops:    collects the task                    SUCCEEDED  stamp=[LADDER-STAMP v=1 sender=orchestrator 
BASELINE   ops:    and applies the config change the ATTACKER asked for SUCCEEDED  allow wrote auth_disabled=true to /tmp/ladde
BASELINE   provenance:  rung 3's label is true and is about one hop SUCCEEDED  delivered [LADDER-STAMP v=1 sender=orchestra
BASELINE   broker state: the world-effect landed        SUCCEEDED  the broker spent its credential: auth_disabl
ENFORCED   reader: still free to talk to its peer, and still delegating SUCCEEDED  delivered to=orchestrator
ENFORCED   orch:   REFUSED - granted is not contained in the caller's own set (claim 2) BLOCKED    not-delivered granted is not contained in th
ENFORCED   orch:   the delegation it actually holds goes through, narrowed again SUCCEEDED  delivered to=ops
ENFORCED   ops:    receives the task, stamped with the whole chain SUCCEEDED  stamp=[LADDER-STAMP v=2 sender=orchestrator 
ENFORCED   ops:    REFUSED - the key is outside the goal's allowlist (claim 4) BLOCKED    deny key 'auth_disabled' is outside the capa
ENFORCED   ops:    its own work is unaffected - the check is on the action, not the sandbox SUCCEEDED  allow cpu=12% mem=41% (fetched with cred:***
ENFORCED   the label:   every hop, written by the runtime, in the message ops was handed SUCCEEDED  delivered [LADDER-STAMP v=2 sender=orchestra
ENFORCED   the sink:    origin, hops, taint and the effective capability SUCCEEDED  goal=svcX-latency-2026-08-17 chain=user>read
ENFORCED   claim 3:     a hop that is in no chain is authorized against nothing SUCCEEDED  deny hop 'nobody' is not in any chain for th
ENFORCED   broker state: nothing was written            SUCCEEDED  auth_disabled lines in /tmp/ladder-253477/br
CONTROL    reader: fetches the same URL, now serving the page nobody edited SUCCEEDED  http_200 body=PROXY_ALLOW WIKI INCIDENT 4471
CONTROL    reader: forwards it                          SUCCEEDED  delivered to=orchestrator
CONTROL    orch:   tasks ops, delegating what it holds and no more SUCCEEDED  delivered to=ops
CONTROL    ops:    collects the task                    SUCCEEDED  stamp=[LADDER-STAMP v=2 sender=orchestrator 
CONTROL    ops:    and applies the config change - the privileged call SUCCEEDS SUCCEEDED  allow wrote cache_size=512 to /tmp/ladder-25
CONTROL    ops:    and its own work still works         SUCCEEDED  allow cpu=12% mem=41% (fetched with cred:***
CONTROL    broker state: the system still does its job  SUCCEEDED  cache_size=512 in /tmp/ladder-253477/broker-
ENFORCED-2 reader: reads the identical page from the labeled mount - rung 2's bit flips SUCCEEDED  read 2665 bytes decode=none
ENFORCED-2 orch:   launders it: writes the message to an unlabeled file and re-sends from there SUCCEEDED  wrote 2759 bytes of a peer's message to an u
ENFORCED-2 ops:    REFUSED, and this time by rung 2's gate inside the runtime BLOCKED    EPERM(1) Operation not permitted
ENFORCED-2 ops:    its OWN legitimate tool, refused too - inherited taint is per-sandbox BLOCKED    EPERM(1) Operation not permitted
ENFORCED-2 ops:    still reaches its allowlisted host - neither mechanism is a kill switch SUCCEEDED  http_200 body=PROXY_ALLOW WIKI INCIDENT 4471
ENFORCED-2 claim 1:     the orchestrator read nothing untrusted and stamped the truth anyway SUCCEEDED  delivered [LADDER-STAMP v=2 sender=orchestra
ENFORCED-2 runsc log:   the middle hop's sentry accumulating a chain it was not told about SUCCEEDED  LADDER CHAIN extend sender=reader inbound="r
ENFORCED-2 runsc log:   rung 2's gate, unchanged, refusing before the bytes leave SUCCEEDED  LADDER DENY sink=/broker/broker.sock taint=s
ENFORCED-3 ops:    still refused - rung 3's taint bit still travels, so rung 2's gate still fires BLOCKED    EPERM(1) Operation not permitted
ENFORCED-3 the loss:    the bit crossed three hops without rung 4; the origin did not SUCCEEDED  delivered [LADDER-STAMP v=1 sender=orchestra
ENFORCED-3 the sink:    records that nothing attested the chain, instead of inventing one SUCCEEDED  goal=svcX-latency-2026-08-17 chain=user>read
ENFORCED-3 the finding:  the key allowlist is broker logic and does not need the flag at all SUCCEEDED  deny key 'auth_disabled' is outside the capa
ENFORCED-4 reader: reads an attacker-authored page that asks for the RIGHT fix SUCCEEDED  read 2253 bytes decode=none
ENFORCED-4 ops:    is tasked with write_config cache_size 512 - inside the goal's allowlist SUCCEEDED  stamp=[LADDER-STAMP v=2 sender=orchestrator 
ENFORCED-4 ops:    REFUSED anyway - rung 2's gate does not look at the parameter BLOCKED    EPERM(1) Operation not permitted
ENFORCED-4 the collision: the two layers disagree, and the blunt one wins SUCCEEDED  allow in scope for goal svcX-latency-2026-08
ENFORCED-4 the cost:      a correct, in-scope, authorized change was refused SUCCEEDED  no new cache_size line in /tmp/ladder-253477
ENFORCED-5 host: the chain this sandbox has accumulated, read from outside it SUCCEEDED  LADDER chain chain=true hops=reader>orchestr
ENFORCED-5 host: and a path this sandbox has no mount for, learned from a peer's stamp SUCCEEDED  origin="/untrusted/injected-page.txt"
----------------------------------------------------------------------------------------------------------------------

the three sandboxes, in full:
  reader tools:    (none)
  orch tools:      (none)
  ops tools:      read_metrics,write_config
  ops egress:     wiki.corp
  goal keys:      cache_size, pool_max, timeout_ms   (auth_disabled is not one)
  channel:        /peer/peer.sock in all three, three different host sockets
every one of those is the same under BASELINE and ENFORCED. What changes is that
the call at the end is authorized against the goal instead of against the caller.

broker log:  /tmp/ladder-253477/broker4.log
postbox log: /tmp/ladder-253477/postbox4.log
chaind log:  /tmp/ladder-253477/chaind4.log
config file: /tmp/ladder-253477/broker-config.txt
runsc log:   /tmp/ladder-runsc

checks: 45 passed, 0 failed
claim exercised: a privileged call is authorized against the chain that produced it -- the capability the user's goal minted, attenuated at every hop and never widened, plus the accumulated taint and origin the runtime stamps -- so an out-of-scope parameter is refused at the broker and a middle hop's attempt to widen what it passes on is refused at the relay, while the identical chain asking for an in-scope key still works
RESULT: PASS
