# rung 5a passing transcript
# captured: 2026-08-17T12:48:55-04:00
# gVisor:   runsc version release-20260810.0-67-ge81dec090e2b-dirty  (rung 4's binary, UNCHANGED -- rung 5a adds no runsc flag)
# kernel:   Linux 6.8.0-1010-intel
# repo:     11787eb35 on ladder (tag rung-4)
# go:       go1.26.3 building ladder/common/fed with GOPROXY=off
# command:  make demo RUNG=5a
#
# One docker runtime is registered, and it is rung 4's:
#   ladder-chain  --ladder-taint --ladder-untrusted-paths=/untrusted
#                 --ladder-privileged-sinks=/broker --ladder-attest
#                 --ladder-peer-channels=/peer --ladder-chain
#                 --debug-log=/tmp/ladder-runsc/%ID%.%COMMAND%.log
#
# The two "hosts" are two federation proxies on loopback aliases, each with its own
# registry, key, capability authority, relay and brokers:
#   hosta 127.0.0.1:14801   hostb 127.0.0.2:14802   MITM 127.0.0.3:14803
# No root, no network namespaces, no second machine, no internet. See rung5a/README.md,
# "Two hosts on one box".
#
# Values that differ between runs and are not assertions: the enrolment codes (a fresh
# 24-bit SPAKE2 code per ceremony), the key fingerprints, the mailbox port, the sequence
# numbers (wall-clock milliseconds), and every timing figure.
rung 5a demo - two hosts, standing services, and a stamp that has to cross a network
runtime:  ladder-chain (rungs 2-4's flags; rung 5a adds no runsc flag at all)
rung 5a demo - two hosts, standing services, and a stamp that has to cross a network
runtime:  ladder-chain (rungs 2-4's flags; rung 5a adds no runsc flag at all)
hosts:    hosta=127.0.0.1:14801  hostb=127.0.0.2:14802  (loopback aliases, no root, no netns)
goal:     svcX-latency-2026-08-17, minted on host A only


=== building ===

images:   ladder/agent-enforced:rung5a, ladder/world:rung5a
binary:   /tmp/ladder-253477/f5a/ladder-fed (go1.26.3, offline from the module cache)

=== starting the world ===

world up: wiki.corp=169.254.0.10  evil.example.com=169.254.0.20  metrics.local=169.254.0.30:9101

=== enrolment: the one human-pace step, once per host, ever ===

KEY host=hosta key=sha256:4c9f8fc7a8c2ea4d file=/tmp/ladder-253477/f5a/a/key
KEY host=hostb key=sha256:c778e655258d4288 file=/tmp/ladder-253477/f5a/b/key
KEY host=impostor key=sha256:2ba347f7493105bc file=/tmp/ladder-253477/f5a/impostor.key
mailbox:  ws://127.0.0.1:40299/ws  (self-hosted; no public relay is contacted)
  operator transcribes: 1-mosquito-berserk-equipment   (SPAKE2 over the mailbox; 24 bits, one guess)
  ENROLL-ACCEPTED host=hosta key=sha256:4c9f8fc7a8c2ea4d runtime=rung-4 epoch=1 addr=127.0.0.1:14801 registry=/tmp/ladder-253477/f5a/b/registry
  ENROLL-BASIS an operator vouched once. Nothing here attested that this host runs the enforcement; that is the next rung.
  operator transcribes: 1-candidate-backfield-vagabond   (SPAKE2 over the mailbox; 24 bits, one guess)
  ENROLL-ACCEPTED host=hostb key=sha256:c778e655258d4288 runtime=rung-4 epoch=1 addr=127.0.0.2:14802 registry=/tmp/ladder-253477/f5a/a/registry
  ENROLL-BASIS an operator vouched once. Nothing here attested that this host runs the enforcement; that is the next rung.
  two ceremonies, 285ms of machine time. The impostor's key was minted and
  never enrolled anywhere -- that single absence is the whole of claim 2.

  host B's registry, which is the entire trust root of this federation:
    {"host":"hosta","key":"B/l0gyOda4NEkvDYTEIJ0Osr5flfeQ8LnhHmHDum/5s=","address":"127.0.0.1:14801","runtime_version":"rung-4","policy_epoch":1,"enrolled_at":"2026-08-17T16:48:04Z","ceremony":"spake2/magic-wormhole, 3-word code"}

=== deriving three sandboxes from three manifests ===

MANIFEST FIELD   VALUE                              BECOMES
------------------------------------------------------------------------------------------------------------
task_id          trigger                            names the network, the proxy, the broker and the scratch dir
agent_image      ladder/agent-enforced:rung5a       the same image every task uses
network_allow                                       --allow on this task's own proxy
broker_tools                                        --tools on this task's own broker
mounts.scratch   rw                                 /scratch, under this task's directory only
mounts.broker    none                               /broker, this task's own socket -- 'none' means no tools at all
mounts.untrusted none                               /untrusted, labeled untrusted by the runtime (rung 2)
mounts.peer      rw                                 /peer, this task's mailbox -- and its stamped identity (rung 3)
ttl_seconds      90                                 hard timeout around the sandbox

MANIFEST FIELD   VALUE                              BECOMES
------------------------------------------------------------------------------------------------------------
task_id          reader                             names the network, the proxy, the broker and the scratch dir
agent_image      ladder/agent-enforced:rung5a       the same image every task uses
network_allow    wiki.corp                          --allow on this task's own proxy
broker_tools                                        --tools on this task's own broker
mounts.scratch   rw                                 /scratch, under this task's directory only
mounts.broker    none                               /broker, this task's own socket -- 'none' means no tools at all
mounts.untrusted ro                                 /untrusted, labeled untrusted by the runtime (rung 2)
mounts.peer      rw                                 /peer, this task's mailbox -- and its stamped identity (rung 3)
ttl_seconds      150                                hard timeout around the sandbox

MANIFEST FIELD   VALUE                              BECOMES
------------------------------------------------------------------------------------------------------------
task_id          ops                                names the network, the proxy, the broker and the scratch dir
agent_image      ladder/agent-enforced:rung5a       the same image every task uses
network_allow    wiki.corp                          --allow on this task's own proxy
broker_tools     read_metrics, write_config         --tools on this task's own broker
mounts.scratch   rw                                 /scratch, under this task's directory only
mounts.broker    rw                                 /broker, this task's own socket -- 'none' means no tools at all
mounts.untrusted none                               /untrusted, labeled untrusted by the runtime (rung 2)
mounts.peer      rw                                 /peer, this task's mailbox -- and its stamped identity (rung 3)
ttl_seconds      150                                hard timeout around the sandbox

host A  trigger: no egress, no tools, no untrusted mount. Holds the goal's capability.
host A  reader:  fetches wiki.corp, has an untrusted mount, holds no tools.
host B  ops:     broker socket (read_metrics, write_config), no untrusted mount.
reader and ops are STANDING: started once, left up, addressed by whoever has a mailbox.

the ops service's deploy-time role, held by host B's proxy and by nothing else:
    read_metrics   no key constraint
    write_config   keys cache_size, pool_max, timeout_ms
  A capability arriving from another host is refused before delivery if it is not
  inside that list. It is a ROLE, not a task -- which is what rung 5a gives up.

=== check 1 of 5: BASELINE - --ladder-fed off, plain TCP between the hosts ===

[BASELINE] trigger: addresses a service that is already running, and delegates the goal => SUCCEEDED (expected)
          delivered to=reader

[BASELINE] reader: fetches the page nobody edited - it asks for cache_size 512, which is in scope => SUCCEEDED (expected)
          http_200 body=PROXY_ALLOW WIKI INCIDENT 4471 -- service X latency ========

[BASELINE] reader: relays its findings to a service on the other host => SUCCEEDED (expected)
          delivered to=ops

[BASELINE] mitm:   reads the sentry's own stamp in the clear - the field rungs 3-4 made unforgeable => SUCCEEDED (expected)
          1786985294.192 read from=reader@hosta to=ops seq=1786985294191 stamp="LADDER-STAMP v=2 sender=reader taint=0 grants= chain=trigger>reader origin=-" body="LADDER-CAP {\"tools\":{\"read_metrics\":{},\"write_confi

[BASELINE] mitm:   rewrites the request AND the capability behind it - neither is signed => SUCCEEDED (expected)
          1786985294.193 rewrite capability claim "\"pool_max\"" -> "\"auth_disabled\"" (nothing signed it either)

[BASELINE] ops:    applies the config change the MITM asked for, not the one the reader sent => SUCCEEDED (expected)
          allow wrote auth_disabled=true to /tmp/ladder-253477/broker-config.txt

[BASELINE] broker state: the world-effect landed => SUCCEEDED (expected)
          the broker spent its credential: auth_disabled=true

[BASELINE] impostor: a host nobody enrolled delivers a forged clean stamp, and it is accepted => SUCCEEDED (expected)
          the receiver accepted it: no check refused this message

  Every mechanism rungs 0-4 built is still on and still correct. The reader's stamp
  was truthful, the delegation was attenuated, the capability authority approved it.
  None of that is a statement about the wire, and the wire is where the attack is.


=== check 2 of 5: ENFORCED - --ladder-fed on ===

[ENFORCED] claim 2: an unenrolled runtime cannot deliver a message at all => BLOCKED (expected)
          handshake-refused -- the peer refused this connection after the handshake: CRYPTO_ERROR 0x12a (remote): tls: bad certificate

[ENFORCED]         and host B says why, before any stream exists => SUCCEEDED (expected)
          handshake decision=deny reason=server: peer public key sha256:2ba347f7493105bc is not in the enrollment registry (enrolled: [hosta])

[ENFORCED] mitm:   in the same position, relaying the same exchange, and reading none of it => BLOCKED (expected)
          relayed=1 datagrams bytes=1280 readable-ladder-tokens=0 sample=" NID\\8c"

  the per-message checks, one at a time. Every one of these is run by an ENROLLED
  sender that chose to misbehave -- once the channel is up, a network attacker
  cannot produce any of them, and saying otherwise would overclaim the transport.

[ENFORCED]         it alters the taint bit in the sentry stamp after signing => BLOCKED (expected)
          signature-invalid -- signature-invalid: envelope is not signed by the key that authenticated this connection (hosta)

[ENFORCED]         it alters the payload after signing => BLOCKED (expected)
          body-tampered -- body-tampered: body sha256 is 81d1e5fa5ce094a8, envelope says b3f5944df52d3542

[ENFORCED]         it re-sends a valid envelope on the same connection => BLOCKED (expected)
          replayed -- replayed: sequence 1786985303334 from hosta/reader has already been delivered on this channel

[ENFORCED]         it re-sends a captured envelope on a fresh connection => BLOCKED (expected)
          not-exporter-bound -- not-exporter-bound: envelope is bound to a different channel; exporter "EXPORTER-ladder-fed" output does not match this 

[ENFORCED]         it backdates the expiry => BLOCKED (expected)
          expired -- expired: envelope expired at 2026-08-17T16:47:23Z (now 2026-08-17T16:48:23Z)

[ENFORCED]         it presents a binding from a different channel => BLOCKED (expected)
          not-exporter-bound -- not-exporter-bound: envelope is bound to a different channel; exporter "EXPORTER-ladder-fed" output does not match this 

[ENFORCED]         it appends a second message after the payload (rung 3's attack, on the wire) => BLOCKED (expected)
          trailing-bytes -- trailing-bytes: 767 byte(s) after the payload; one exchange per stream, trailing bytes are a second message

[ENFORCED]         it asserts a capability outside the ops service's deploy-time role => BLOCKED (expected)
          outside-role-scope -- tool "write_config": key(s) auth_disabled are outside the role (role allows: cache_size,pool_max,timeout_ms)

[ENFORCED] ops:    its legitimate work is unaffected - the substrate refuses messages, not services => SUCCEEDED (expected)
          allow wrote cache_size=512 to /tmp/ladder-253477/broker-config.txt

[ENFORCED] broker state: nothing was written => SUCCEEDED (expected)
          auth_disabled lines in /tmp/ladder-253477/broker-config.txt: 1 before, 1 after

  host B's rejection counters: body-tampered=1 expired=1 handshake-refused=1 not-exporter-bound=2 outside-role-scope=1 replayed=1 signature-invalid=1 trailing-bytes=1
  replay cache:                peers=1 window=1024 accepted=4 rejected=1 bytes~144
  Every one of those refusals happened in the PROXY. The postbox was not contacted,
  the capability authority was not asked, and the ops sandbox saw no bytes.


=== check 3 of 5: CONTROL - the legitimate cross-host chain, and what it costs ===

[CONTROL] trigger: three requests to one already-running service, no human step between them => SUCCEEDED (expected)
          delivered to=reader

[CONTROL] reader: handles them as they arrive - it was up before the first one => SUCCEEDED (expected)
          stamp=[LADDER-STAMP v=2 sender=trigger taint=0 grants= chain=trigger origin=-] body=LADDER-CAP {"tools":{"read_metrics":{},"write_config":{"keys

[CONTROL] reader: relays across the federation, over the connection already established => SUCCEEDED (expected)
          delivered to=ops

[CONTROL] ops:    receives the task on the other host => SUCCEEDED (expected)
          stamp=[LADDER-STAMP v=2 sender=reader taint=0 grants= chain=trigger>reader origin=-] body=LADDER-CAP {"tools":{"read_metrics":{},"write_config":{"keys

[CONTROL] ops:    and applies the config change - the privileged call SUCCEEDS => SUCCEEDED (expected)
          allow wrote cache_size=512 to /tmp/ladder-253477/broker-config.txt

[CONTROL] broker state: the system still does its job, one host over => SUCCEEDED (expected)
          cache_size=512 in /tmp/ladder-253477/broker-config.txt

  the capability as host B holds it, imported from a host it enrolled once:
    goal=svcX-latency-2026-08-17 chain=user@hosta>trigger@hosta>reader@hosta>ops origin=- taint=0 effective=read_metrics write_config{cache_size,pool_max} imported-from=reader@hosta

  the burst. Section 6 asks for this as an assertion rather than as prose:
    MEASUREMENT                LOOPBACK, UNLOADED BOX -- TREAT AS A FLOOR
    cold handshake (Connect)   n=5    min=  4.24ms  median=  4.38ms  max=  5.29ms
    stream-open (Converse)     n=20   min=   127us  median=   307us  max=   644us
    exchange, end to end       n=20   min=  3.01ms  median=  4.09ms  max=  4.64ms
    burst of 32, concurrent    32/32 completed in 25.15ms (786us each, amortized)
[CONTROL] agent-pace: the acceptance criterion, run rather than asserted => SUCCEEDED (expected)
          32/32 concurrent exchanges completed on one warm connection, no human step on the path

  One enrolment ceremony per host, ever. Everything above it -- the handshake, every
  stream, the whole burst -- ran with nobody watching. That is the requirement the
  spec states as a design forcer, and it is why 0-RTT could be left off.


=== check 4 of 5: ENFORCED-2 - rung 4's chain, across the network ===

[ENFORCED-2] reader: reads the injected page from the LABELED mount - rung 2's bit flips => SUCCEEDED (expected)
          read 2544 bytes decode=none

[ENFORCED-2] reader: relays it across the federation, stamped by its own kernel => SUCCEEDED (expected)
          delivered to=ops

[ENFORCED-2] ops:    REFUSED on a machine that observed none of the facts the refusal rests on => BLOCKED (expected)
          EPERM(1) Operation not permitted

[ENFORCED-2] claim 4: the inner stamp arrived byte-for-byte; host B has no mount for that path => SUCCEEDED (expected)
          delivered [LADDER-STAMP v=2 sender=reader taint=1 grants= chain=trigger>reader origin=/untrusted/incident-page.txt]

[ENFORCED-2] runsc log:   rung 2's gate, unchanged, on the far side of a network hop => SUCCEEDED (expected)
          LADDER DENY sink=/broker/broker.sock taint=set source=peer:reader reason=write-to-privileged-sink-from-tainted-sandbox

[ENFORCED-2] the crack:   logged where it happens, not only in the README => SUCCEEDED (expected)
          import hop=ops from=reader@hosta host=hosta chain=user@hosta>trigger@hosta>reader@hosta>ops taint=1 origin=/untrusted/incident-page.txt basis=enrollment-not-attestation

  what host B's capability authority holds, and where every part of it came from:
    goal=svcX-latency-2026-08-17 chain=user@hosta>trigger@hosta>reader@hosta>ops origin=/untrusted/incident-page.txt taint=1 effective=read_metrics write_config{cache_size,pool_max} imported-from=reader@hosta
  Host B has no copy of the goal, no mount for that path, and no way to observe the
  read. It has the reader's sentry's word, wrapped in an envelope host A's proxy
  signed with a key host B enrolled once -- and nothing that attests either runtime.


=== check 5 of 5: ENFORCED-3 - the recycle policy, and its residual window ===

[ENFORCED-3] claim 5: the proxy READ the runtime's taint bit and asked for a rebirth => SUCCEEDED (expected)
          reader taint 1 true 3.766s (service, reason, exchanges, tainted, age)

[ENFORCED-3] claim 5: nothing anywhere cleared the bit in place - the sandbox holding it is gone => SUCCEEDED (expected)
          reader generation 1 was tainted; generation 2 is not, and it is a different sandbox

[ENFORCED-3]         and the runtime agrees, on the other host, in a stamp nobody host-side wrote => SUCCEEDED (expected)
          delivered [LADDER-STAMP v=2 sender=reader taint=0 grants= chain=trigger>reader origin=-]

  the residual window, as a number rather than a caveat:
    the reader served 1 exchange(s) after the read that tainted it and before
    the recycle request. With --recycle-on-taint that is the minimum: the proxy learns
    the bit from the stamp on an outbound message, so at least one message has already
    gone out. A count or age threshold makes it larger, never smaller.
  And what recycling does NOT do: the poisoned page is still on the wiki and still in
  the labeled mount. A reborn service that reads it again is tainted again immediately.
  Recycling bounds how long a taint lasts. It does not touch why it happened.


=== check-by-check ===


CONFIG     CHANNEL / PROBE                              OUTCOME    EVIDENCE
----------------------------------------------------------------------------------------------------------------------
BASELINE   trigger: addresses a service that is already running, and delegates the goal SUCCEEDED  delivered to=reader
BASELINE   reader: fetches the page nobody edited - it asks for cache_size 512, which is in scope SUCCEEDED  http_200 body=PROXY_ALLOW WIKI INCIDENT 4471
BASELINE   reader: relays its findings to a service on the other host SUCCEEDED  delivered to=ops
BASELINE   mitm:   reads the sentry's own stamp in the clear - the field rungs 3-4 made unforgeable SUCCEEDED  1786985294.192 read from=reader@hosta to=ops
BASELINE   mitm:   rewrites the request AND the capability behind it - neither is signed SUCCEEDED  1786985294.193 rewrite capability claim "\"p
BASELINE   ops:    applies the config change the MITM asked for, not the one the reader sent SUCCEEDED  allow wrote auth_disabled=true to /tmp/ladde
BASELINE   broker state: the world-effect landed        SUCCEEDED  the broker spent its credential: auth_disabl
BASELINE   impostor: a host nobody enrolled delivers a forged clean stamp, and it is accepted SUCCEEDED  the receiver accepted it: no check refused t
ENFORCED   claim 2: an unenrolled runtime cannot deliver a message at all BLOCKED    handshake-refused -- the peer refused this c
ENFORCED           and host B says why, before any stream exists SUCCEEDED  handshake decision=deny reason=server: peer 
ENFORCED   mitm:   in the same position, relaying the same exchange, and reading none of it BLOCKED    relayed=1 datagrams bytes=1280 readable-ladd
ENFORCED           it alters the taint bit in the sentry stamp after signing BLOCKED    signature-invalid -- signature-invalid: enve
ENFORCED           it alters the payload after signing  BLOCKED    body-tampered -- body-tampered: body sha256 
ENFORCED           it re-sends a valid envelope on the same connection BLOCKED    replayed -- replayed: sequence 1786985303334
ENFORCED           it re-sends a captured envelope on a fresh connection BLOCKED    not-exporter-bound -- not-exporter-bound: en
ENFORCED           it backdates the expiry              BLOCKED    expired -- expired: envelope expired at 2026
ENFORCED           it presents a binding from a different channel BLOCKED    not-exporter-bound -- not-exporter-bound: en
ENFORCED           it appends a second message after the payload (rung 3's attack, on the wire) BLOCKED    trailing-bytes -- trailing-bytes: 767 byte(s
ENFORCED           it asserts a capability outside the ops service's deploy-time role BLOCKED    outside-role-scope -- tool "write_config": k
ENFORCED   ops:    its legitimate work is unaffected - the substrate refuses messages, not services SUCCEEDED  allow wrote cache_size=512 to /tmp/ladder-25
ENFORCED   broker state: nothing was written            SUCCEEDED  auth_disabled lines in /tmp/ladder-253477/br
CONTROL    trigger: three requests to one already-running service, no human step between them SUCCEEDED  delivered to=reader
CONTROL    reader: handles them as they arrive - it was up before the first one SUCCEEDED  stamp=[LADDER-STAMP v=2 sender=trigger taint
CONTROL    reader: relays across the federation, over the connection already established SUCCEEDED  delivered to=ops
CONTROL    ops:    receives the task on the other host  SUCCEEDED  stamp=[LADDER-STAMP v=2 sender=reader taint=
CONTROL    ops:    and applies the config change - the privileged call SUCCEEDS SUCCEEDED  allow wrote cache_size=512 to /tmp/ladder-25
CONTROL    broker state: the system still does its job, one host over SUCCEEDED  cache_size=512 in /tmp/ladder-253477/broker-
CONTROL    agent-pace: the acceptance criterion, run rather than asserted SUCCEEDED  32/32 concurrent exchanges completed on one 
ENFORCED-2 reader: reads the injected page from the LABELED mount - rung 2's bit flips SUCCEEDED  read 2544 bytes decode=none
ENFORCED-2 reader: relays it across the federation, stamped by its own kernel SUCCEEDED  delivered to=ops
ENFORCED-2 ops:    REFUSED on a machine that observed none of the facts the refusal rests on BLOCKED    EPERM(1) Operation not permitted
ENFORCED-2 claim 4: the inner stamp arrived byte-for-byte; host B has no mount for that path SUCCEEDED  delivered [LADDER-STAMP v=2 sender=reader ta
ENFORCED-2 runsc log:   rung 2's gate, unchanged, on the far side of a network hop SUCCEEDED  LADDER DENY sink=/broker/broker.sock taint=s
ENFORCED-2 the crack:   logged where it happens, not only in the README SUCCEEDED  import hop=ops from=reader@hosta host=hosta 
ENFORCED-3 claim 5: the proxy READ the runtime's taint bit and asked for a rebirth SUCCEEDED  reader taint 1 true 3.766s (service, reason,
ENFORCED-3 claim 5: nothing anywhere cleared the bit in place - the sandbox holding it is gone SUCCEEDED  reader generation 1 was tainted; generation 
ENFORCED-3         and the runtime agrees, on the other host, in a stamp nobody host-side wrote SUCCEEDED  delivered [LADDER-STAMP v=2 sender=reader ta
----------------------------------------------------------------------------------------------------------------------

what crossed the network, and who could vouch for each layer:
  LAYER                              WRITTEN BY             UNFORGEABLE BY
  sentry stamp (256 bytes, inner)    the sending kernel     the sending agent
  federation envelope (outer)        host A's proxy         anyone off-host
  channel binding                    the TLS exporter       anyone on another channel
  the enrolment itself               an operator, once      nobody -- see the crack

hosta fed log:  /tmp/ladder-253477/f5a/a/fed.log
hostb fed log:  /tmp/ladder-253477/f5a/b/fed.log
mitm log:       /tmp/ladder-253477/f5a/mitm.log
chaind logs:    /tmp/ladder-253477/f5a/a/chaind.log  /tmp/ladder-253477/f5a/b/chaind.log
postbox logs:   /tmp/ladder-253477/f5a/a/postbox.log /tmp/ladder-253477/f5a/b/postbox.log
config file:    /tmp/ladder-253477/broker-config.txt
latency table:  /tmp/ladder-253477/f5a/out/bench.txt

checks: 37 passed, 0 failed
claim exercised: cross-host agent traffic flows only through enrolled runtime proxies over a mutually authenticated, replay-resistant, channel-bound substrate -- an unenrolled host is refused at the handshake, a man in the middle reads and rewrites the baseline and neither reads nor rewrites the enforced one, an enrolled sender cannot tamper with, replay, backdate, unbind or append to a message, the sentry's taint and origin cross the network intact so the far side's runtime still refuses, and a tainted standing service is bounded by destroy-and-restart rather than cleared in place
RESULT: PASS
