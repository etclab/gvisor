# ripwire quality acks v1 — written by --quality-ack; a finding stays suppressed until it worsens past its acked magnitude
# format: ack <kind> <16-hex-key> <ackNow> [cid=<16-hex-content-id>] [by=<scope that acked it>] <reason to end of line> — one per line, kept SORTED by (kind,key) on every write (merge-friendly)
ack duplication 0ec3ef96effcc75e 37 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack duplication 14ff19aba977ac93 37 by=policyfile.go,refvalsfile.go What the two loaders still share is the if err != nil around one call to the shared signature check or file reader and one call to that document's own parser; every check, prefix and refusal they used to copy is now signDocument, verifySignedDocument and readSignedDocumentPair. Collapsing further needs the unexported parsers passed as values, which hides their call edge from the graph and files them as dead code.
ack duplication 18b306b0a1d12e8c 39 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack duplication 1c02b73769701ceb 162 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry. Re-acked in ticket 21 under the ref-pair view, because ticket 20's keys hash a member set and ticket 21 moved builders into internal/fixture; every member of every row here was resolved to a definition in a _test.go file first, and accept at tunneld/tunneld.go is not the accept in these groups. | prior: Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack duplication 23583370f2f2aef6 46 by=internal/fixture Ticket 21 moved the platform, verifier, author and acceptance builders that nine test files had each copied into attest/internal/fixture, which no non-test package imports. Each of these rows pairs one of those builders with a per-package test helper of the same t.Helper-then-one-call shape over different types; every member was resolved to a definition in internal/fixture/fixture.go or in a _test.go file before this entry was written. Nothing in this entry is production code; internal/fixture is test support that sits outside _test.go only because a package may not import another package's test files.
ack duplication 280d2c80b40c6317 210 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry. Re-acked in ticket 21 under the ref-pair view, because ticket 20's keys hash a member set and ticket 21 moved builders into internal/fixture; every member of every row here was resolved to a definition in a _test.go file first, and accept at tunneld/tunneld.go is not the accept in these groups. | prior: Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack duplication 29f78d295f84e14f 42 by=internal/fixture Ticket 21 moved the platform, verifier, author and acceptance builders that nine test files had each copied into attest/internal/fixture, which no non-test package imports. Each of these rows pairs one of those builders with a per-package test helper of the same t.Helper-then-one-call shape over different types; every member was resolved to a definition in internal/fixture/fixture.go or in a _test.go file before this entry was written. Nothing in this entry is production code; internal/fixture is test support that sits outside _test.go only because a package may not import another package's test files.
ack duplication 315e38ced48863a9 223 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry. Re-acked in ticket 21 under the ref-pair view, because ticket 20's keys hash a member set and ticket 21 moved builders into internal/fixture; every member of every row here was resolved to a definition in a _test.go file first, and accept at tunneld/tunneld.go is not the accept in these groups. | prior: Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack duplication 338a0349b92ac503 203 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry. Re-acked in ticket 21 under the ref-pair view, because ticket 20's keys hash a member set and ticket 21 moved builders into internal/fixture; every member of every row here was resolved to a definition in a _test.go file first, and accept at tunneld/tunneld.go is not the accept in these groups. | prior: Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack duplication 34adcef950468681 28 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry. Re-acked in ticket 21 under the ref-pair view, because ticket 20's keys hash a member set and ticket 21 moved builders into internal/fixture; every member of every row here was resolved to a definition in a _test.go file first, and accept at tunneld/tunneld.go is not the accept in these groups.
ack duplication 364ea2fb8039c475 31 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry. Re-acked in ticket 21 under the ref-pair view, because ticket 20's keys hash a member set and ticket 21 moved builders into internal/fixture; every member of every row here was resolved to a definition in a _test.go file first, and accept at tunneld/tunneld.go is not the accept in these groups.
ack duplication 38cf9ef9de6a4b27 188 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry. Re-acked in ticket 21 under the ref-pair view, because ticket 20's keys hash a member set and ticket 21 moved builders into internal/fixture; every member of every row here was resolved to a definition in a _test.go file first, and accept at tunneld/tunneld.go is not the accept in these groups.
ack duplication 4a11adb6bb325e43 28 by=internal/fixture Ticket 21 moved the platform, verifier, author and acceptance builders that nine test files had each copied into attest/internal/fixture, which no non-test package imports. Each of these rows pairs one of those builders with a per-package test helper of the same t.Helper-then-one-call shape over different types; every member was resolved to a definition in internal/fixture/fixture.go or in a _test.go file before this entry was written. Nothing in this entry is production code; internal/fixture is test support that sits outside _test.go only because a package may not import another package's test files.
ack duplication 4a138dc1e67105ae 272 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry. Re-acked in ticket 21 under the ref-pair view, because ticket 20's keys hash a member set and ticket 21 moved builders into internal/fixture; every member of every row here was resolved to a definition in a _test.go file first, and accept at tunneld/tunneld.go is not the accept in these groups. | prior: Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack duplication 4fe9ca7391c0b674 39 by=internal/fixture Ticket 21 moved the platform, verifier, author and acceptance builders that nine test files had each copied into attest/internal/fixture, which no non-test package imports. Each of these rows pairs one of those builders with a per-package test helper of the same t.Helper-then-one-call shape over different types; every member was resolved to a definition in internal/fixture/fixture.go or in a _test.go file before this entry was written. Nothing in this entry is production code; internal/fixture is test support that sits outside _test.go only because a package may not import another package's test files.
ack duplication 52415e115b988b68 48 by=internal/fixture Ticket 21 moved the platform, verifier, author and acceptance builders that nine test files had each copied into attest/internal/fixture, which no non-test package imports. Each of these rows pairs one of those builders with a per-package test helper of the same t.Helper-then-one-call shape over different types; every member was resolved to a definition in internal/fixture/fixture.go or in a _test.go file before this entry was written. Nothing in this entry is production code; internal/fixture is test support that sits outside _test.go only because a package may not import another package's test files.
ack duplication 52b340fd82516990 64 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack duplication 5a505407a9d7f826 53 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack duplication 5dbbee5391b254b1 143 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack duplication 629f6d2079e385d1 42 by=snpfake/snpfake.go productFms parses a product line through the SNP fake's library table and tdxHex decodes a hex string in a test; the shared shape is one call, one error check, one return. The SNP fake is a test fixture and the two do different things, so there is nothing to share.
ack duplication 695e829a3f73dfe7 48 by=internal/fixture Ticket 21 moved the platform, verifier, author and acceptance builders that nine test files had each copied into attest/internal/fixture, which no non-test package imports. Each of these rows pairs one of those builders with a per-package test helper of the same t.Helper-then-one-call shape over different types; every member was resolved to a definition in internal/fixture/fixture.go or in a _test.go file before this entry was written. Nothing in this entry is production code; internal/fixture is test support that sits outside _test.go only because a package may not import another package's test files.
ack duplication 70bb65735d7e9655 27 by=internal/fixture Ticket 21 moved the platform, verifier, author and acceptance builders that nine test files had each copied into attest/internal/fixture, which no non-test package imports. Each of these rows pairs one of those builders with a per-package test helper of the same t.Helper-then-one-call shape over different types; every member was resolved to a definition in internal/fixture/fixture.go or in a _test.go file before this entry was written. Nothing in this entry is production code; internal/fixture is test support that sits outside _test.go only because a package may not import another package's test files.
ack duplication 775a4fbeed944d65 242 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry. Re-acked in ticket 21 under the ref-pair view, because ticket 20's keys hash a member set and ticket 21 moved builders into internal/fixture; every member of every row here was resolved to a definition in a _test.go file first, and accept at tunneld/tunneld.go is not the accept in these groups.
ack duplication 787757fdcd9c1e56 150 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry. Re-acked in ticket 21 under the ref-pair view, because ticket 20's keys hash a member set and ticket 21 moved builders into internal/fixture; every member of every row here was resolved to a definition in a _test.go file first, and accept at tunneld/tunneld.go is not the accept in these groups. | prior: Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack duplication 8230e3be92a5c18c 35 by=internal/fixture/fixture.go,ratls/ratls.go fixture.Detail is testsupport_test.go's detail moved into attest/internal/fixture and exported in ticket 21, body unchanged (git show 205fd2154:attest/testsupport_test.go). Its only regression is identity: a clone key hashes the member set, so the move renamed the group. ratls.refusal is the production unwrap that makes every refusal typed at that package's edge and manufactures ReasonMalformedEvidence when it is not; it shares the errors.As shape and nothing else.
ack duplication 85098087a119dbc0 39 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack duplication 876417535e97cd30 24 by=internal/fixture Ticket 21 moved the platform, verifier, author and acceptance builders that nine test files had each copied into attest/internal/fixture, which no non-test package imports. Each of these rows pairs one of those builders with a per-package test helper of the same t.Helper-then-one-call shape over different types; every member was resolved to a definition in internal/fixture/fixture.go or in a _test.go file before this entry was written. Nothing in this entry is production code; internal/fixture is test support that sits outside _test.go only because a package may not import another package's test files.
ack duplication 8db9d87d4c2e9352 241 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry. Re-acked in ticket 21 under the ref-pair view, because ticket 20's keys hash a member set and ticket 21 moved builders into internal/fixture; every member of every row here was resolved to a definition in a _test.go file first, and accept at tunneld/tunneld.go is not the accept in these groups. | prior: Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack duplication 955b1a08be29ef6f 39 by=internal/fixture Ticket 21 moved the platform, verifier, author and acceptance builders that nine test files had each copied into attest/internal/fixture, which no non-test package imports. Each of these rows pairs one of those builders with a per-package test helper of the same t.Helper-then-one-call shape over different types; every member was resolved to a definition in internal/fixture/fixture.go or in a _test.go file before this entry was written. Nothing in this entry is production code; internal/fixture is test support that sits outside _test.go only because a package may not import another package's test files.
ack duplication 980f2310a473e2ac 62 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack duplication 999cb3f11de95087 49 by=verify/snp.go,verify/tdx.go The error-to-refusal mapping the two checkAuthentic bodies shared is now refuseUnrooted in verify/shared.go; what remains is each vendor library's own options struct and entry point (sevverify.Options/SnpAttestationContext vs tdxverify.Options/TdxQuote). Sharing that would mean wrapping two third-party verification libraries behind a common interface, a contract change this ticket does not make.
ack duplication 9c7e995d3fdbee87 24 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry. Re-acked in ticket 21 under the ref-pair view, because ticket 20's keys hash a member set and ticket 21 moved builders into internal/fixture; every member of every row here was resolved to a definition in a _test.go file first, and accept at tunneld/tunneld.go is not the accept in these groups.
ack duplication ac5752a905a76954 46 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack duplication b4fe8762d9f0599c 23 by=provision/provision.go,policyfile.go,refvalsfile.go,verify/tdxcollateral.go Four two-line sentinel constructors in three packages, each binding attest.Refuse to its own package's sentinel. Folding the two in package attest still leaves one-liners cloning each other across packages, and deleting the named constructors makes every call site spell its sentinel, which is worse. Left as they are.
ack duplication b51ee9589ebb13d5 272 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry. Re-acked in ticket 21 under the ref-pair view, because ticket 20's keys hash a member set and ticket 21 moved builders into internal/fixture; every member of every row here was resolved to a definition in a _test.go file first, and accept at tunneld/tunneld.go is not the accept in these groups. | prior: Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack duplication b7e91712911fc2fb 53 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack duplication bbaef8d46acdffcb 37 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack duplication bfbce75b5d04f879 242 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry. Re-acked in ticket 21 under the ref-pair view, because ticket 20's keys hash a member set and ticket 21 moved builders into internal/fixture; every member of every row here was resolved to a definition in a _test.go file first, and accept at tunneld/tunneld.go is not the accept in these groups. | prior: Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack duplication c14ea3e1b10cce33 26 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack duplication c403566b180f8ef1 64 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack duplication c439475272adfd3f 32 by=internal/fixture/fixture.go,refusal.go fixture.Detail is testsupport_test.go's detail moved into attest/internal/fixture and exported in ticket 21, body unchanged (git show 205fd2154:attest/testsupport_test.go). Its only regression is identity: a clone key hashes the member set, so the move renamed the group. What it shares with attest.ReasonOf is the errors.As unwrap shape; one returns the operator-facing text for a test failure, the other a Reason, and they live in different packages.
ack duplication c54e878cc3ffb6b2 207 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry. Re-acked in ticket 21 under the ref-pair view, because ticket 20's keys hash a member set and ticket 21 moved builders into internal/fixture; every member of every row here was resolved to a definition in a _test.go file first, and accept at tunneld/tunneld.go is not the accept in these groups. | prior: Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack duplication c83aea7943f7f28a 23 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry. Re-acked in ticket 21 under the ref-pair view, because ticket 20's keys hash a member set and ticket 21 moved builders into internal/fixture; every member of every row here was resolved to a definition in a _test.go file first, and accept at tunneld/tunneld.go is not the accept in these groups.
ack duplication cd25f45996665345 64 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack duplication da5f8d3c0a26ba68 224 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry. Re-acked in ticket 21 under the ref-pair view, because ticket 20's keys hash a member set and ticket 21 moved builders into internal/fixture; every member of every row here was resolved to a definition in a _test.go file first, and accept at tunneld/tunneld.go is not the accept in these groups. | prior: Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack duplication dcdc3a5aa3a69993 161 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry. Re-acked in ticket 21 under the ref-pair view, because ticket 20's keys hash a member set and ticket 21 moved builders into internal/fixture; every member of every row here was resolved to a definition in a _test.go file first, and accept at tunneld/tunneld.go is not the accept in these groups.
ack duplication e23157f9c402fdc4 29 by=verify/tdx.go anyOf searches [][]byte with bytes.Equal and contains searches []string with ==; []byte is not comparable so no one generic helper serves both, and contains lives in the test files of two other packages, one of them the frozen attest/tsm. Different functions of the same shape, left as they are.
ack duplication e592b3bd50d46b1f 98 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack duplication eae1bed4d6b4c94d 218 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry. Re-acked in ticket 21 under the ref-pair view, because ticket 20's keys hash a member set and ticket 21 moved builders into internal/fixture; every member of every row here was resolved to a definition in a _test.go file first, and accept at tunneld/tunneld.go is not the accept in these groups. | prior: Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack duplication eb4ddaba9e426213 27 by=verify/tdx.go anyOf searches [][]byte with bytes.Equal and contains searches []string with ==; []byte is not comparable so no one generic helper serves both, and contains lives in the test files of two other packages, one of them the frozen attest/tsm. Different functions of the same shape, left as they are.
ack duplication efbcbca152d93157 34 by=policyfile.go,refvalsfile.go What the two loaders still share is the if err != nil around one call to the shared signature check or file reader and one call to that document's own parser; every check, prefix and refusal they used to copy is now signDocument, verifySignedDocument and readSignedDocumentPair. Collapsing further needs the unexported parsers passed as values, which hides their call edge from the graph and files them as dead code. Re-acked in ticket 21: the pair is unchanged, the exports became loadPolicy and loadReferenceValueSet, and a clone key hashes the member set, so ticket 20's key died on the rename.
ack duplication f4866c9e1bb75a40 47 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack duplication f5a65a348138d36e 20 by=internal/fixture Ticket 21 moved the platform, verifier, author and acceptance builders that nine test files had each copied into attest/internal/fixture, which no non-test package imports. Each of these rows pairs one of those builders with a per-package test helper of the same t.Helper-then-one-call shape over different types; every member was resolved to a definition in internal/fixture/fixture.go or in a _test.go file before this entry was written. Nothing in this entry is production code; internal/fixture is test support that sits outside _test.go only because a package may not import another package's test files.
ack duplication f600a27d127da45d 90 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry. Re-acked in ticket 21 under the ref-pair view, because ticket 20's keys hash a member set and ticket 21 moved builders into internal/fixture; every member of every row here was resolved to a definition in a _test.go file first, and accept at tunneld/tunneld.go is not the accept in these groups.
ack duplication fa74ca4f513c574e 242 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry. Re-acked in ticket 21 under the ref-pair view, because ticket 20's keys hash a member set and ticket 21 moved builders into internal/fixture; every member of every row here was resolved to a definition in a _test.go file first, and accept at tunneld/tunneld.go is not the accept in these groups. | prior: Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack duplication fb0b31f45108f499 180 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack duplication fe576c38e2d8aae8 24 by=testsupport_test.go,cmd/attest-tool/acquire.go newAcquirer is ticket 20's helper moved byte-for-byte from cmd/acquire-evidence/main.go into cmd/attest-tool/acquire.go (git show 205fd2154:attest/cmd/acquire-evidence/main.go); defaultConfig is a test-only snpfake.Config literal that lost its Now field when ticket 21 cut the fake to what a test reaches. What is left is two one-line struct constructors of unrelated types, one in a test file and one in a command, with no package between them: ticket 20 accepted closedPolicy | tcbParts for the same reason.
ack new-clone-of-reused-helper 0ec3ef96effcc75e 19 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack new-clone-of-reused-helper 14ff19aba977ac93 5 by=policyfile.go,refvalsfile.go What the two loaders still share is the if err != nil around one call to the shared signature check or file reader and one call to that document's own parser; every check, prefix and refusal they used to copy is now signDocument, verifySignedDocument and readSignedDocumentPair. Collapsing further needs the unexported parsers passed as values, which hides their call edge from the graph and files them as dead code.
ack new-clone-of-reused-helper 280d2c80b40c6317 5 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack new-clone-of-reused-helper 34adcef950468681 25 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry. Re-acked in ticket 21 under the ref-pair view, because ticket 20's keys hash a member set and ticket 21 moved builders into internal/fixture; every member of every row here was resolved to a definition in a _test.go file first, and accept at tunneld/tunneld.go is not the accept in these groups.
ack new-clone-of-reused-helper 364ea2fb8039c475 25 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry. Re-acked in ticket 21 under the ref-pair view, because ticket 20's keys hash a member set and ticket 21 moved builders into internal/fixture; every member of every row here was resolved to a definition in a _test.go file first, and accept at tunneld/tunneld.go is not the accept in these groups.
ack new-clone-of-reused-helper 5dbbee5391b254b1 7 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack new-clone-of-reused-helper 70bb65735d7e9655 25 by=internal/fixture Ticket 21 moved the platform, verifier, author and acceptance builders that nine test files had each copied into attest/internal/fixture, which no non-test package imports. Each of these rows pairs one of those builders with a per-package test helper of the same t.Helper-then-one-call shape over different types; every member was resolved to a definition in internal/fixture/fixture.go or in a _test.go file before this entry was written. Nothing in this entry is production code; internal/fixture is test support that sits outside _test.go only because a package may not import another package's test files.
ack new-clone-of-reused-helper 876417535e97cd30 5 by=internal/fixture Ticket 21 moved the platform, verifier, author and acceptance builders that nine test files had each copied into attest/internal/fixture, which no non-test package imports. Each of these rows pairs one of those builders with a per-package test helper of the same t.Helper-then-one-call shape over different types; every member was resolved to a definition in internal/fixture/fixture.go or in a _test.go file before this entry was written. Nothing in this entry is production code; internal/fixture is test support that sits outside _test.go only because a package may not import another package's test files.
ack new-clone-of-reused-helper 980f2310a473e2ac 32 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack new-clone-of-reused-helper 9c7e995d3fdbee87 6 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry. Re-acked in ticket 21 under the ref-pair view, because ticket 20's keys hash a member set and ticket 21 moved builders into internal/fixture; every member of every row here was resolved to a definition in a _test.go file first, and accept at tunneld/tunneld.go is not the accept in these groups.
ack new-clone-of-reused-helper ac5752a905a76954 25 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack new-clone-of-reused-helper b4fe8762d9f0599c 4 by=provision/provision.go,policyfile.go,refvalsfile.go,verify/tdxcollateral.go Four two-line sentinel constructors in three packages, each binding attest.Refuse to its own package's sentinel. Folding the two in package attest still leaves one-liners cloning each other across packages, and deleting the named constructors makes every call site spell its sentinel, which is worse. Left as they are.
ack new-clone-of-reused-helper b7e91712911fc2fb 30 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack new-clone-of-reused-helper bbaef8d46acdffcb 19 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack new-clone-of-reused-helper c14ea3e1b10cce33 25 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack new-clone-of-reused-helper c439475272adfd3f 14 by=internal/fixture/fixture.go,refusal.go fixture.Detail is testsupport_test.go's detail moved into attest/internal/fixture and exported in ticket 21, body unchanged (git show 205fd2154:attest/testsupport_test.go). Its only regression is identity: a clone key hashes the member set, so the move renamed the group. What it shares with attest.ReasonOf is the errors.As unwrap shape; one returns the operator-facing text for a test failure, the other a Reason, and they live in different packages.
ack new-clone-of-reused-helper c83aea7943f7f28a 25 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry. Re-acked in ticket 21 under the ref-pair view, because ticket 20's keys hash a member set and ticket 21 moved builders into internal/fixture; every member of every row here was resolved to a definition in a _test.go file first, and accept at tunneld/tunneld.go is not the accept in these groups.
ack new-clone-of-reused-helper e592b3bd50d46b1f 21 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack new-clone-of-reused-helper eae1bed4d6b4c94d 11 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack new-clone-of-reused-helper f5a65a348138d36e 6 by=internal/fixture Ticket 21 moved the platform, verifier, author and acceptance builders that nine test files had each copied into attest/internal/fixture, which no non-test package imports. Each of these rows pairs one of those builders with a per-package test helper of the same t.Helper-then-one-call shape over different types; every member was resolved to a definition in internal/fixture/fixture.go or in a _test.go file before this entry was written. Nothing in this entry is production code; internal/fixture is test support that sits outside _test.go only because a package may not import another package's test files.
ack new-clone-of-reused-helper f600a27d127da45d 7 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry. Re-acked in ticket 21 under the ref-pair view, because ticket 20's keys hash a member set and ticket 21 moved builders into internal/fixture; every member of every row here was resolved to a definition in a _test.go file first, and accept at tunneld/tunneld.go is not the accept in these groups.
ack new-clone-of-reused-helper fe576c38e2d8aae8 49 by=testsupport_test.go,cmd/attest-tool/acquire.go newAcquirer is ticket 20's helper moved byte-for-byte from cmd/acquire-evidence/main.go into cmd/attest-tool/acquire.go (git show 205fd2154:attest/cmd/acquire-evidence/main.go); defaultConfig is a test-only snpfake.Config literal that lost its Now field when ticket 21 cut the fake to what a test reaches. What is left is two one-line struct constructors of unrelated types, one in a test file and one in a command, with no package between them: ticket 20 accepted closedPolicy | tcbParts for the same reason.
ack verbosity dbdb900f3ae62860 62 cid=cbccdc4d8c02e3ad by=refvalsfile.go MarshalReferenceValueSet grew because version 4 added a vendor tag and a second vendor: the switch's two arms share only vendor and policy_digest and render structurally different documents, so there is nothing to share and splitting them would only hide which fields each vendor's document carries.
