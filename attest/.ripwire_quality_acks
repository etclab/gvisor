# ripwire quality acks v1 — written by --quality-ack; a finding stays suppressed until it worsens past its acked magnitude
# format: ack <kind> <16-hex-key> <ackNow> [cid=<16-hex-content-id>] [by=<scope that acked it>] <reason to end of line> — one per line, kept SORTED by (kind,key) on every write (merge-friendly)
ack duplication 0489f4cc22e7f39f 55 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack duplication 048a8e30c5008fac 59 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack duplication 0540359d58cd2a38 55 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack duplication 0581948dd9034f6b 55 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack duplication 0ec3ef96effcc75e 37 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack duplication 14ff19aba977ac93 37 by=policyfile.go,refvalsfile.go What the two loaders still share is the if err != nil around one call to the shared signature check or file reader and one call to that document's own parser; every check, prefix and refusal they used to copy is now signDocument, verifySignedDocument and readSignedDocumentPair. Collapsing further needs the unexported parsers passed as values, which hides their call edge from the graph and files them as dead code.
ack duplication 1710bde8d91428f9 44 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack duplication 1774effcd8ff0e3b 48 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack duplication 18b306b0a1d12e8c 39 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack duplication 1c02b73769701ceb 158 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack duplication 1c5c8e76624c1111 47 by=tdxfake/tdxfake.go,refvalsfile_test.go The TDX fake's writeFile joins a directory and name, writes mode 0644 and returns a wrapped error for its own package; the test's writeFile writes a read-only 0444 file and fails the test. Same os.WriteFile idiom, different contracts, one of them in a test file of another package.
ack duplication 280d2c80b40c6317 208 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack duplication 315e38ced48863a9 221 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack duplication 316f9b3162de2439 59 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack duplication 338a0349b92ac503 201 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack duplication 39985cc1285979b8 50 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack duplication 3b85f28771c29085 50 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack duplication 41656bc281e8a7ba 50 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack duplication 4a138dc1e67105ae 270 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack duplication 4ccf0b16f252016d 59 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack duplication 52b340fd82516990 64 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack duplication 5a505407a9d7f826 53 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack duplication 5dbbee5391b254b1 143 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack duplication 629f6d2079e385d1 42 by=snpfake/snpfake.go productFms parses a product line through the SNP fake's library table and tdxHex decodes a hex string in a test; the shared shape is one call, one error check, one return. The SNP fake is a test fixture and the two do different things, so there is nothing to share.
ack duplication 66a42624b117759f 46 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack duplication 77d1a88ee9d2126b 47 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack duplication 787757fdcd9c1e56 148 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack duplication 78bc267ad66c05e7 44 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack duplication 79eed0ae08a07e83 46 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack duplication 7cbb072893b0eef2 50 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack duplication 83f81724f2419852 55 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack duplication 85098087a119dbc0 39 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack duplication 8db9d87d4c2e9352 239 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack duplication 96d20464428a48de 50 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack duplication 980f2310a473e2ac 62 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack duplication 999cb3f11de95087 49 by=verify/snp.go,verify/tdx.go The error-to-refusal mapping the two checkAuthentic bodies shared is now refuseUnrooted in verify/shared.go; what remains is each vendor library's own options struct and entry point (sevverify.Options/SnpAttestationContext vs tdxverify.Options/TdxQuote). Sharing that would mean wrapping two third-party verification libraries behind a common interface, a contract change this ticket does not make.
ack duplication 9b958dc686897eec 31 by=verify/snp.go,cmd/tunneld/egress_test.go closedPolicy builds the test's closed attest.Policy literal and tcbParts maps an attest.TCB onto the library's kds.TCBParts; both are one-line struct constructors of unrelated types. The same idiom, not the same function; there is nothing to share.
ack duplication 9e3c4131ef6d3f88 31 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack duplication a6e4a24b640fcade 46 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack duplication ac5752a905a76954 46 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack duplication acc5d9fbd5cf38d1 53 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack duplication b4fe8762d9f0599c 23 by=provision/provision.go,policyfile.go,refvalsfile.go,verify/tdxcollateral.go Four two-line sentinel constructors in three packages, each binding attest.Refuse to its own package's sentinel. Folding the two in package attest still leaves one-liners cloning each other across packages, and deleting the named constructors makes every call site spell its sentinel, which is worse. Left as they are.
ack duplication b51ee9589ebb13d5 270 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack duplication b7e91712911fc2fb 53 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack duplication b8427bebaa6c0408 61 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack duplication b93388cda30c6a5f 61 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack duplication bbaef8d46acdffcb 37 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack duplication bc096bb9f444e400 50 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack duplication bcd09f795302e8c1 44 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack duplication bf779d2001788896 59 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack duplication bfbce75b5d04f879 240 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack duplication c14ea3e1b10cce33 26 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack duplication c351b99c42e2457c 50 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack duplication c403566b180f8ef1 64 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack duplication c54e878cc3ffb6b2 205 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack duplication cb030f2a25b9afe4 50 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack duplication cd25f45996665345 64 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack duplication cd9ac53b8cb7da2b 54 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack duplication d46aab4e39783587 50 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack duplication d65c9866f7f1640b 59 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack duplication d687d940bc37d4d7 34 by=policyfile.go,refvalsfile.go What the two loaders still share is the if err != nil around one call to the shared signature check or file reader and one call to that document's own parser; every check, prefix and refusal they used to copy is now signDocument, verifySignedDocument and readSignedDocumentPair. Collapsing further needs the unexported parsers passed as values, which hides their call edge from the graph and files them as dead code.
ack duplication da5f8d3c0a26ba68 222 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack duplication dc7b6c694b2c92b8 48 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack duplication dd0fbb861f55c392 44 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack duplication e1786305105366cf 50 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack duplication e23157f9c402fdc4 29 by=verify/tdx.go anyOf searches [][]byte with bytes.Equal and contains searches []string with ==; []byte is not comparable so no one generic helper serves both, and contains lives in the test files of two other packages, one of them the frozen attest/tsm. Different functions of the same shape, left as they are.
ack duplication e592b3bd50d46b1f 98 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack duplication e8cc125151fda4b0 50 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack duplication eae1bed4d6b4c94d 216 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack duplication eb4ddaba9e426213 27 by=verify/tdx.go anyOf searches [][]byte with bytes.Equal and contains searches []string with ==; []byte is not comparable so no one generic helper serves both, and contains lives in the test files of two other packages, one of them the frozen attest/tsm. Different functions of the same shape, left as they are.
ack duplication f139132fa076acd3 188 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack duplication f4866c9e1bb75a40 47 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack duplication fa74ca4f513c574e 240 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack duplication fb0b31f45108f499 180 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack duplication fc8af1430f92fd51 44 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack new-clone-of-reused-helper 0489f4cc22e7f39f 7 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack new-clone-of-reused-helper 048a8e30c5008fac 6 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack new-clone-of-reused-helper 0540359d58cd2a38 7 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack new-clone-of-reused-helper 0581948dd9034f6b 7 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack new-clone-of-reused-helper 0ec3ef96effcc75e 19 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack new-clone-of-reused-helper 14ff19aba977ac93 5 by=policyfile.go,refvalsfile.go What the two loaders still share is the if err != nil around one call to the shared signature check or file reader and one call to that document's own parser; every check, prefix and refusal they used to copy is now signDocument, verifySignedDocument and readSignedDocumentPair. Collapsing further needs the unexported parsers passed as values, which hides their call edge from the graph and files them as dead code.
ack new-clone-of-reused-helper 1710bde8d91428f9 26 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack new-clone-of-reused-helper 1c5c8e76624c1111 3 by=tdxfake/tdxfake.go,refvalsfile_test.go The TDX fake's writeFile joins a directory and name, writes mode 0644 and returns a wrapped error for its own package; the test's writeFile writes a read-only 0444 file and fails the test. Same os.WriteFile idiom, different contracts, one of them in a test file of another package.
ack new-clone-of-reused-helper 280d2c80b40c6317 5 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack new-clone-of-reused-helper 316f9b3162de2439 7 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack new-clone-of-reused-helper 39985cc1285979b8 6 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack new-clone-of-reused-helper 3b85f28771c29085 5 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack new-clone-of-reused-helper 41656bc281e8a7ba 6 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack new-clone-of-reused-helper 4ccf0b16f252016d 25 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack new-clone-of-reused-helper 5dbbee5391b254b1 7 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack new-clone-of-reused-helper 66a42624b117759f 25 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack new-clone-of-reused-helper 77d1a88ee9d2126b 25 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack new-clone-of-reused-helper 78bc267ad66c05e7 25 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack new-clone-of-reused-helper 7cbb072893b0eef2 5 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack new-clone-of-reused-helper 83f81724f2419852 7 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack new-clone-of-reused-helper 96d20464428a48de 6 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack new-clone-of-reused-helper 980f2310a473e2ac 32 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack new-clone-of-reused-helper 9e3c4131ef6d3f88 49 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack new-clone-of-reused-helper ac5752a905a76954 25 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack new-clone-of-reused-helper acc5d9fbd5cf38d1 25 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack new-clone-of-reused-helper b4fe8762d9f0599c 4 by=provision/provision.go,policyfile.go,refvalsfile.go,verify/tdxcollateral.go Four two-line sentinel constructors in three packages, each binding attest.Refuse to its own package's sentinel. Folding the two in package attest still leaves one-liners cloning each other across packages, and deleting the named constructors makes every call site spell its sentinel, which is worse. Left as they are.
ack new-clone-of-reused-helper b7e91712911fc2fb 30 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack new-clone-of-reused-helper b8427bebaa6c0408 6 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack new-clone-of-reused-helper b93388cda30c6a5f 6 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack new-clone-of-reused-helper bbaef8d46acdffcb 19 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack new-clone-of-reused-helper bc096bb9f444e400 6 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack new-clone-of-reused-helper bcd09f795302e8c1 25 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack new-clone-of-reused-helper bf779d2001788896 5 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack new-clone-of-reused-helper c14ea3e1b10cce33 25 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack new-clone-of-reused-helper c351b99c42e2457c 25 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack new-clone-of-reused-helper cb030f2a25b9afe4 6 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack new-clone-of-reused-helper d46aab4e39783587 6 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack new-clone-of-reused-helper d65c9866f7f1640b 10 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack new-clone-of-reused-helper d687d940bc37d4d7 11 by=policyfile.go,refvalsfile.go What the two loaders still share is the if err != nil around one call to the shared signature check or file reader and one call to that document's own parser; every check, prefix and refusal they used to copy is now signDocument, verifySignedDocument and readSignedDocumentPair. Collapsing further needs the unexported parsers passed as values, which hides their call edge from the graph and files them as dead code.
ack new-clone-of-reused-helper dc7b6c694b2c92b8 25 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack new-clone-of-reused-helper e1786305105366cf 6 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack new-clone-of-reused-helper e592b3bd50d46b1f 21 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack new-clone-of-reused-helper e8cc125151fda4b0 5 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack new-clone-of-reused-helper eae1bed4d6b4c94d 11 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack new-clone-of-reused-helper f139132fa076acd3 14 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack new-clone-of-reused-helper fc8af1430f92fd51 25 by=*_test.go Table-driven scenarios written long-hand per refusal reason in the TDX phase's test files: each test names the one input that earns one refusal, and the shared setup lines around it are repeated on purpose so a failing test reads on its own. Test files only; nothing outside _test.go is acked by this entry.
ack short-horizon-churn 35b92885e95326d1 2 cid=c34615c93ae3be21 by=doc.go,policyfile.go,refvals.go,refvalsfile.go,verification.go,export_test.go,policyfile_test.go,refvalsfile_test.go,cmd/tunneld/egress_test.go Ticket 21 item 1 is a rename: six exported names become their unexported spellings, and every symbol these rows name is one the substitution passed through. The churn is ticket 21 landing on the files ticket 20 had just reshaped, not a second rewrite of any body; --edit-check reports unchanged for each of these symbols and the diff is identifier-for-identifier.
ack short-horizon-churn 6563392a622ea889 2 cid=692d14f127d9aa37 by=doc.go,policyfile.go,refvals.go,refvalsfile.go,verification.go,export_test.go,policyfile_test.go,refvalsfile_test.go,cmd/tunneld/egress_test.go Ticket 21 item 1 is a rename: six exported names become their unexported spellings, and every symbol these rows name is one the substitution passed through. The churn is ticket 21 landing on the files ticket 20 had just reshaped, not a second rewrite of any body; --edit-check reports unchanged for each of these symbols and the diff is identifier-for-identifier.
ack short-horizon-churn 7500220c52f07150 6 cid=43fd87e1886bcdc1 by=doc.go,policyfile.go,refvals.go,refvalsfile.go,verification.go,export_test.go,policyfile_test.go,refvalsfile_test.go,cmd/tunneld/egress_test.go Ticket 21 item 1 is a rename: six exported names become their unexported spellings, and every symbol these rows name is one the substitution passed through. The churn is ticket 21 landing on the files ticket 20 had just reshaped, not a second rewrite of any body; --edit-check reports unchanged for each of these symbols and the diff is identifier-for-identifier.
ack short-horizon-churn 75939fd550cf0040 4 cid=970d59d98df5b968 by=doc.go,policyfile.go,refvals.go,refvalsfile.go,verification.go,export_test.go,policyfile_test.go,refvalsfile_test.go,cmd/tunneld/egress_test.go Ticket 21 item 1 is a rename: six exported names become their unexported spellings, and every symbol these rows name is one the substitution passed through. The churn is ticket 21 landing on the files ticket 20 had just reshaped, not a second rewrite of any body; --edit-check reports unchanged for each of these symbols and the diff is identifier-for-identifier.
ack short-horizon-churn 76ab694aa1b4d559 2 cid=81e6e514bbc0a3df by=doc.go,policyfile.go,refvals.go,refvalsfile.go,verification.go,export_test.go,policyfile_test.go,refvalsfile_test.go,cmd/tunneld/egress_test.go Ticket 21 item 1 is a rename: six exported names become their unexported spellings, and every symbol these rows name is one the substitution passed through. The churn is ticket 21 landing on the files ticket 20 had just reshaped, not a second rewrite of any body; --edit-check reports unchanged for each of these symbols and the diff is identifier-for-identifier.
ack short-horizon-churn 9bac87440ef7fb7b 2 cid=f2c2ed9e38300c98 by=doc.go,policyfile.go,refvals.go,refvalsfile.go,verification.go,export_test.go,policyfile_test.go,refvalsfile_test.go,cmd/tunneld/egress_test.go Ticket 21 item 1 is a rename: six exported names become their unexported spellings, and every symbol these rows name is one the substitution passed through. The churn is ticket 21 landing on the files ticket 20 had just reshaped, not a second rewrite of any body; --edit-check reports unchanged for each of these symbols and the diff is identifier-for-identifier.
ack short-horizon-churn c6f40cd20f8792bd 2 cid=2b13745bd096d690 by=doc.go,policyfile.go,refvals.go,refvalsfile.go,verification.go,export_test.go,policyfile_test.go,refvalsfile_test.go,cmd/tunneld/egress_test.go Ticket 21 item 1 is a rename: six exported names become their unexported spellings, and every symbol these rows name is one the substitution passed through. The churn is ticket 21 landing on the files ticket 20 had just reshaped, not a second rewrite of any body; --edit-check reports unchanged for each of these symbols and the diff is identifier-for-identifier.
ack short-horizon-churn dbdb900f3ae62860 6 cid=f6968c382281472d by=doc.go,policyfile.go,refvals.go,refvalsfile.go,verification.go,export_test.go,policyfile_test.go,refvalsfile_test.go,cmd/tunneld/egress_test.go Ticket 21 item 1 is a rename: six exported names become their unexported spellings, and every symbol these rows name is one the substitution passed through. The churn is ticket 21 landing on the files ticket 20 had just reshaped, not a second rewrite of any body; --edit-check reports unchanged for each of these symbols and the diff is identifier-for-identifier.
ack short-horizon-churn e8a618954bc7f71e 6 cid=1160c4668d6d12c0 by=doc.go,policyfile.go,refvals.go,refvalsfile.go,verification.go,export_test.go,policyfile_test.go,refvalsfile_test.go,cmd/tunneld/egress_test.go Ticket 21 item 1 is a rename: six exported names become their unexported spellings, and every symbol these rows name is one the substitution passed through. The churn is ticket 21 landing on the files ticket 20 had just reshaped, not a second rewrite of any body; --edit-check reports unchanged for each of these symbols and the diff is identifier-for-identifier.
ack verbosity dbdb900f3ae62860 62 cid=cbccdc4d8c02e3ad by=refvalsfile.go MarshalReferenceValueSet grew because version 4 added a vendor tag and a second vendor: the switch's two arms share only vendor and policy_digest and render structurally different documents, so there is nothing to share and splitting them would only hide which fields each vendor's document carries.
